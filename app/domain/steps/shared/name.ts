import { StepDefinition } from "~/domain/steps";

export const stepDefinition: StepDefinition = {
  fields: {
    anrede: { validations: {} },
    titel: { validations: {} },
    name: { validations: {} },
    vorname: { validations: {} },
  },
};
